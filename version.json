{
  "version": "3.8.1",
  "release_date": "2026-05-12",
  "changelog": "Phase 36.1 — фиксы после MCP verify. (1) js/owner-integrations.js: csrfToken fallback на meta[name=csrf-token] (на /owner.php в body нет data-csrf-token, в отличие от /admin/* — settings/mapping submit не работал). (2) db.php createOrderFromAggregator: orders.delivery_details — VARCHAR(255), мой JSON со всеми полями туда не влезал → create_failed. Теперь там короткий human-readable summary (address + phone, mb_substr 255), полный normalized payload — в orders.aggregator_payload (JSON column)."
}
