{
  "version": "3.16.3",
  "release_date": "2026-06-16",
  "changelog": "Phase L102.3 — fix invisible text на 4 опциях admin menu-view block. MCP verify показал rgb(222, 222, 222) (= #dedede) — это broken labus tenant token --text-color (same issue фикснули в Phase L100.12 для minimal view). Fix: hardcoded `#1a1a1a` в `.menu-view-text` color вместо var(--text-color, #1a1a1a) — fallback не помогал т.к. var определён (просто misconfigured). Mobile (375) проверен — layout без overflow, та же fix применима. Все остальные элементы (heading, hint, cards, radio accent, button) уже visible. Single-line fix."
}
