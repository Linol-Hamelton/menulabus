{
  "scripts": {
    "openapi:validate": "node scripts/validate-openapi.mjs",
    "optimize:css": "postcss \"css/*.css\" --dir .tmp-css-optimized --no-map",
    "optimize:css:check": "postcss \"css/*.css\" --dir .tmp-css-check --no-map",
    "optimize:css:replace": "postcss \"css/*.css\" --replace --no-map"
  },
  "devDependencies": {
    "@apidevtools/swagger-parser": "^10.1.1",
    "autoprefixer": "^10.4.24",
    "cssnano": "^7.1.2",
    "esbuild": "^0.27.3",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1"
  }
}
